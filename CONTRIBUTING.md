# 怎么修改这本书

[← 总目录](README.md)

发现错误时，请说清楚哪一章、哪一条、哪里不对，并附原始来源。个人经验可以提供线索，但写成一般建议前还要查证。涉及自己或别人的事情时，别把账户、病历、证件和案件细节公开贴上来。

## 改哪里

编辑 `sources/guide.json`，然后使用 Python 3.10 或更高版本运行：

```sh
python tools/test_validation.py
python tools/build.py
python tools/validate.py
```

`book/*.md`、`sources/README.md`、`docs/ADAPTATION.md` 和 `README.md` 由脚本生成。修改时先改数据，再生成 Markdown，避免正文和来源记录对不上。`sources/upstream.json` 保存原书目录和固定版本；以后跟进原书更新时，重新核对改编表。

来源记录必须有真实访问日期、读取范围和能支持的具体内容。新来源应使用未占用的 ID；既有条目 ID 保持稳定，方便读者链接和两个语言版本以后共享事实记录。

## 怎么写

- 标题直接说做什么，例如「关掉不用的订阅」，少用「管理」「评估」「建立机制」这类空泛说法。
- 「说人话」先讲读者需要知道的结论；「怎么做」列实际步骤；成本、收益和备注分开写。
- 数字要有出处，适用人群和例外不能省。把「有资格申请」写成「一定领得到」，不是简化，是改错了。
- 通用研究可以沿用原书的同一篇论文，核对原文后直接引用。澳洲办事规则查对应主管机构。
- 读者在 GitHub 打开 Markdown 就能看全文。检查目录、条目锚点和来源链接即可，不需要启动网站。

## 发布前

结构校验通过后，仍须阅读变更涉及的来源并核对正文。请在变更中说明法律或政策生效时间、适用资格、费用口径和例外。法规和医学建议的重大变动应接受相关专业人士复核。

修改完成后提交正文、数据和生成结果。GitHub Actions 会重跑内容检查，并检查生成文件有没有漏更新。
