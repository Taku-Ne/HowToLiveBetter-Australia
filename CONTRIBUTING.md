# 怎么修改这本书

[← 总目录](README.md)

纠错请写明章节、条目、哪里不对，并附能支持修改的原始资料。不要公开真实病历、账户、证件或案件隐私。

## 改哪里

正文数据在 `sources/guide.json`。修改数据后运行：

```sh
python tools/test_validation.py
python tools/build.py
python tools/validate.py
```

`book/*.md`、`README.md`、来源索引、逐条对照和 `docs/核实记录/` 由脚本生成；悉尼生活附录由同一数据生成。四篇与原书同名的长文是人工维护的 Markdown，更新时也要补来源记录和交叉链接。

`sources/upstream.json` 保存固定原版及全部位置。31 个章节文件名、章节顺序和 498 个条目位置须保持对应；不要为了避免重复而合并或删题。原书版本将来改变时，需要另作明确迁移。

## 怎么写

- 标题直接说动作，「说人话」解释实际得失，避免只重复标题。
- 固定成本、说人话、收益、证据等级、来源、备注六项；步骤、资格和求救条件都直接可见。
- 通用研究保留人群、时间和统计口径，澳洲制度查具体官方来源，没有对应制度就说明区别。
- 新条目字段包括 `evidence_grade`、`references`、`adaptation` 和 `adaptation_note`；`steps` 可以为空，必要动作写入正文。
- `references` 保存原书转引书目，不冒充独立阅读全文。O 编号记录必须是 `upstream_citation`；独立核实的来源另给 ID，标 `full_page`、`abstract` 或 `pdf`。
- 每个来源的日期和支持内容如实记录。引用必须支撑实际结论，仅同主题不够。

## 发布前

检查原题是否仍被真正回答，研究数字有没有被解释成个人收益承诺，澳洲年龄、身份、期限和费用条件是否齐全。运行结构检查后再审阅生成的 Markdown；检查通过不等于事实已获专业认可。

提交数据、工具和生成结果。GitHub Actions 重建并检测生成文件是否漏更新；仓库直接阅读 Markdown，不生成网站。
